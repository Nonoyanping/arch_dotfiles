# This file is generated by matugen. DO NOT EDIT BY HAND.
#
# Your settings:
# 1. Set status bar to the top
# 2. Apply matugen color theme

# --- Settings ---
set -g status-position top
set -g status "on"
set -g status-justify absolute-centre
set -g status-interval 1

# --- Colors ---
# These are the matugen color variables
set -g @primary "#f6b5c7"
set -g @on_primary "#4e2231"
set -g @secondary "#dfbec6"
set -g @on_secondary "#402a31"
set -g @tertiary "#f6ba8c"
set -g @on_tertiary "#4c2705"
set -g @surface "#171213"
set -g @on_surface "#ebe0e1"
set -g @on_surface_variant "#d5c2c5"
set -g @secondary_container "#584047"
set -g @on_secondary_container "#fad7df"
set -g @tertiary_container "#7d502b"
set -g @on_tertiary_container "#ffffff"

# --- Powerline Glyphs ---
set -g @left_separator ""
set -g @right_separator ""
set -g @left_separator_alt ""
set -g @right_separator_alt ""

# --- Base Statusline ---
set -g status-style "fg=#{@on_surface},bg=#{@surface}"

# --- Left Side ---
# [ Session Name ]
set -g status-left-length 100
set -g status-left-style "fg=#{@on_primary},bg=#{@primary}"
set -g status-left "#[bold] #{@left_separator} #S #{@right_separator_alt} "

# --- Window List (Center) ---
# Inactive Window
setw -g window-status-format "#[fg=#{@on_surface_variant},bg=#{@surface}] #I  #W "
# Active Window
setw -g window-status-current-format "#[fg=#{@on_secondary},bg=#{@secondary}]#{@left_separator_alt} #I  #W #{@right_separator_alt}"

# --- Right Side ---
# [ Date & Time ]
set -g status-right-length 100
set -g status-right-style "fg=#{@on_secondary_container},bg=#{@secondary_container}"
set -g status-right "#[bold] #{@left_separator_alt} %Y-%m-%d  %H:%M #{@right_separator} "

# --- Panes ---
set -g pane-border-style "fg=#{@primary}"
set -g pane-active-border-style "fg=#{@secondary}"

# --- Command Bar/Messages ---
set -g message-style "fg=#{@on_tertiary_container},bg=#{@tertiary_container}"
